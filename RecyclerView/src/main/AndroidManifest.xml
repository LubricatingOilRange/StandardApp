<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.recycler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.multi_item.MultiLevel2Activity" />
        <activity android:name=".ui.multi_item.MultiLevel3Activity" />
        <activity android:name=".ui.refresh.RefreshActivity" />
        <activity android:name=".ui.anim.AnimationActivity" />
        <activity android:name=".ui.decoration.SuspendDecorationActivity" />
        <activity android:name=".ui.decoration.SuspendToolBarActivity"></activity>
    </application>

</manifest>